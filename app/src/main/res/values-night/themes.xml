<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/background_primary</item>
        <item name="colorPrimaryVariant">@color/background_secondary</item>
        <item name="colorOnPrimary">@color/text_color</item>
        <item name="android:textColor">@color/text_color</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/foreground_primary</item>
        <item name="colorSecondaryVariant">@color/foreground_secondary</item>
        <item name="colorOnSecondary">@color/text_color_inverted</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/text_color_inverted</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="AppTheme.FullScreen">
        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>